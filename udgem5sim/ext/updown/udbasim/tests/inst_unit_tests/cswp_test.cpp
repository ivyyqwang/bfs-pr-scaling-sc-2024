#include <gtest/gtest.h>
#include "udlane.hh"
#include "types.hh"
#include "lanetypes.hh"
#include "udaccelerator.hh"

using namespace basim;

class cswp : public ::testing::Test {
 protected:
    size_t num_threads = 2;
    int numLanes = 64;
    UDAccelerator acc0 = UDAccelerator(numLanes, 0, 1); // UDAccelerator(numLanes, UDID/networkID, lmmode (0: accelerator based addressing, 1:bank/lane based addressing))
    int buffsize1 = 1024;
    int buffsize2 = 1024;
};
TEST_F(cswp, Basic_cswp_0){
    acc0.initSetup(0, "testprogs/binaries/cswp_0.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 58773));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 14625110567186154989u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 12022728658927644494u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 14625110567186154989u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 58773, 12022728658927644494u));
    }
}
TEST_F(cswp, Basic_cswp_1){
    acc0.initSetup(0, "testprogs/binaries/cswp_1.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 11080));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 5615015389549808257u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 2793128560742234496u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 13922680083698868812u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 11080, 13922680083698868812u));
    }
}
TEST_F(cswp, Basic_cswp_2){
    acc0.initSetup(0, "testprogs/binaries/cswp_2.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 15498));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 1052315061954042184u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 4577424894342693347u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 16180696544489722028u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 15498, 16180696544489722028u));
    }
}
TEST_F(cswp, Basic_cswp_3){
    acc0.initSetup(0, "testprogs/binaries/cswp_3.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 56402));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 17280972398839709867u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 3992790220452824747u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 17280972398839709867u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 56402, 3992790220452824747u));
    }
}
TEST_F(cswp, Basic_cswp_4){
    acc0.initSetup(0, "testprogs/binaries/cswp_4.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 53490));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 725852119779652033u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 5346179477604165401u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 6975129909816130456u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 53490, 6975129909816130456u));
    }
}
TEST_F(cswp, Basic_cswp_5){
    acc0.initSetup(0, "testprogs/binaries/cswp_5.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 54017));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 15356405422427728493u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 11984187289523765753u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 1379786887950697887u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 54017, 1379786887950697887u));
    }
}
TEST_F(cswp, Basic_cswp_6){
    acc0.initSetup(0, "testprogs/binaries/cswp_6.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 53752));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 11230844035583191321u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 7917603281202200031u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 11230844035583191321u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 53752, 7917603281202200031u));
    }
}
TEST_F(cswp, Basic_cswp_7){
    acc0.initSetup(0, "testprogs/binaries/cswp_7.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 38757));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 11377738654807636755u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 15078147636955740600u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 11377738654807636755u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 38757, 15078147636955740600u));
    }
}
TEST_F(cswp, Basic_cswp_8){
    acc0.initSetup(0, "testprogs/binaries/cswp_8.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 6223));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 8628144449950626227u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 6390275851308816026u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 9673658516249547446u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 6223, 9673658516249547446u));
    }
}
TEST_F(cswp, Basic_cswp_9){
    acc0.initSetup(0, "testprogs/binaries/cswp_9.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 21709));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 11312800934173582082u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 9663417337881301528u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 5063371907980089677u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 21709, 5063371907980089677u));
    }
}
TEST_F(cswp, Basic_cswp_10){
    acc0.initSetup(0, "testprogs/binaries/cswp_10.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 41149));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 2568653745037243089u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 16517747186986829303u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 12755171866769214902u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 41149, 12755171866769214902u));
    }
}
TEST_F(cswp, Basic_cswp_11){
    acc0.initSetup(0, "testprogs/binaries/cswp_11.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 54242));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 4202556144917815623u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 6815997011547428330u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 7078252005636460594u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 54242, 7078252005636460594u));
    }
}
TEST_F(cswp, Basic_cswp_12){
    acc0.initSetup(0, "testprogs/binaries/cswp_12.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 17778));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 4932448389867428177u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 3677522956679779753u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 1248224351741618067u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 17778, 1248224351741618067u));
    }
}
TEST_F(cswp, Basic_cswp_13){
    acc0.initSetup(0, "testprogs/binaries/cswp_13.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 15077));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 14299795448664321593u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 12913474053321086240u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 12309137217733125376u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 15077, 12309137217733125376u));
    }
}
TEST_F(cswp, Basic_cswp_14){
    acc0.initSetup(0, "testprogs/binaries/cswp_14.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 12105));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 2553327383813369129u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 4148700618923930160u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 2553327383813369129u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 12105, 4148700618923930160u));
    }
}
TEST_F(cswp, Basic_cswp_15){
    acc0.initSetup(0, "testprogs/binaries/cswp_15.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 18026));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 12169008790440973284u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 3524140801463673804u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 12169008790440973284u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 18026, 3524140801463673804u));
    }
}
TEST_F(cswp, Basic_cswp_16){
    acc0.initSetup(0, "testprogs/binaries/cswp_16.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 13845));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 6486516447602228915u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 1825278993078050564u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 10261396977078037268u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 13845, 10261396977078037268u));
    }
}
TEST_F(cswp, Basic_cswp_17){
    acc0.initSetup(0, "testprogs/binaries/cswp_17.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 44987));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 2762955334683027148u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 3052381716067256537u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 2762955334683027148u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 44987, 3052381716067256537u));
    }
}
TEST_F(cswp, Basic_cswp_18){
    acc0.initSetup(0, "testprogs/binaries/cswp_18.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 12531));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 17999530959115554735u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 12152907932720445974u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 7306816031941584357u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 12531, 7306816031941584357u));
    }
}
TEST_F(cswp, Basic_cswp_19){
    acc0.initSetup(0, "testprogs/binaries/cswp_19.bin", 0);
    int numop = 9;
    eventword_t ev0(0);
    ev0.setNumOperands(numop);
    operands_t op0(numop);
    word_t *data = new word_t[numop];
    data[0] = reinterpret_cast<word_t>((uint64_t)8);
    data[1] = reinterpret_cast<word_t>((uint64_t)9);
    data[2] = reinterpret_cast<word_t>((uint64_t)10);
    data[3] = reinterpret_cast<word_t>((uint64_t)11);
    data[4] = reinterpret_cast<word_t>((uint64_t)12);
    data[5] = reinterpret_cast<word_t>((uint64_t)13);
    data[6] = reinterpret_cast<word_t>((uint64_t)14);
    data[7] = reinterpret_cast<word_t>((uint64_t)15);
    
    op0.setData(data);
    eventoperands_t eops(&ev0, &op0);
    for(auto i = 0; i < numLanes; i++)
        acc0.pushEventOperands(eops, i);
    while(!acc0.isIdle()){
        acc0.simulate(2); // 2 is num stick
    }
    // Checks specific for tests being written
    for(auto i = 0; i < numLanes; i++){
        EXPECT_TRUE(acc0.testReg(i, RegId::X16, 29678));
        EXPECT_TRUE(acc0.testReg(i, RegId::X18, 12871029651661134831u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X19, 7500057107591949299u));
        EXPECT_TRUE(acc0.testReg(i, RegId::X17, 4125382394706001333u));
        EXPECT_TRUE(acc0.testMem((i<<16) + 29678, 4125382394706001333u));
    }
}
